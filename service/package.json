{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^18.7.2"
  },
  "dependencies": {
    "@furystack/core": "^11.0.8",
    "@furystack/filesystem-store": "^3.0.8",
    "@furystack/inject": "^7.0.2",
    "@furystack/logging": "^3.0.6",
    "@furystack/repository": "^6.0.8",
    "@furystack/rest-service": "^6.1.0",
    "tslib": "^2.4.0"
  }
}
