{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^17.0.29"
  },
  "dependencies": {
    "@furystack/core": "^10.4.10",
    "@furystack/filesystem-store": "^2.0.34",
    "@furystack/inject": "^5.0.7",
    "@furystack/logging": "^2.0.91",
    "@furystack/repository": "^5.0.34",
    "@furystack/rest-service": "^5.0.4",
    "tslib": "^2.4.0"
  }
}
